services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: frontend-cem
    environment:
      - TZ=Asia/Bangkok
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - cem_network

networks:
  cem_network:
    external: true
